name: main-workflow
on:
  workflow_dispatch
jobs:
  build:
    runs-on: self
    steps:
      - name: checking version
        run: sudo docker version
      - name: echo
        run: echo "passed"
     
